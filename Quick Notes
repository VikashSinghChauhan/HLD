⚡ Quick Numbers

HTTP POST (polling every 1s): ~500–1000 bytes/request → 60 KB per user per minute. With 100K users → ~6 GB/min traffic.

WebSocket frame: ~10–50 bytes per update → 600 bytes/min/user. With 100K users → ~60 MB/min traffic.
👉 That’s ~100x more efficient.

✅ Rule of Thumb

If updates are frequent (sub-second to few seconds) → use WebSockets (or SSE if one-way).

If updates are rare (every few mins, on demand) → simple HTTP API is fine.


SSE :::::::


SSE (Server-Sent Events) keeps a persistent HTTP connection open from the client → server, and the server can stream events continuously over it.

The client makes a single GET /stream request.

The server responds with Content-Type: text/event-stream and never closes the connection (unless explicitly needed).

Server keeps writing events like:

data: {"lat":12.9,"lon":77.6}
data: {"lat":12.91,"lon":77.61}


Connection stays open until:

Client closes it, or

Network/timeout happens.


Design OTT/Live stream :: https://docs.google.com/document/d/1yinOF64-w7h2Zywcq-t5yoKmpKq2YJD1QPSM7C8aFz8/edit?tab=t.0#heading=h.q14ghi85s8u3
